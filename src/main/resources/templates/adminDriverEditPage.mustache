<!DOCTYPE HTML>
<head>
    <title>Getting Started: Serving Web Content</title>
    <meta http-equiv="Content-Type" content="text/html; charset=UTF-8"/>
</head>

<body>

<a href="/admin/drivers">
    <button>Turn back</button>
</a>

<b>Driver Editor</b>

<div>Current driver</div>
<div>
    <a>DriverId {{id}}</a>
    <br/>
    <a>DriverName {{name}}</a>
    <br/>
    <a>DriverCategory {{category}}</a>
    <br/>
    <a>DriverSalary {{salary}}</a>
    <br/>
    <a>DriverStatus {{status}}</a>
    <br/>
    <a>DriverMileage {{mileage}}</a>
    <br/>
    <a>Completed Orders Today {{todayOrders}}</a>
    <br/>
    <a>Total Completed Orders {{totalOrders}}</a>
</div>

<a href="/admin/drivers/{{id}}/deleteDriver">
    <button>Delete driver</button>
</a>

<div>Change driver status</div>
<form method="post">
    <label>
        <select name="select" size="2" multiple>
            <option selected value="s1">isFree</option>
            <option value="s2">isAtWork</option>
        </select>
    </label>
    <input type="hidden" name="_csrf" value="{{_csrf.token}}">
    <button type="submit">Change Driver Status</button>
</form>

<div>Change Drivers Salary</div>
<form method="post" action="/admin/drivers/{{id}}/salary">
    <input type="number" name="salary" placeholder="AdditionalDriverSalary">
    <input type="hidden" name="_csrf" value="{{_csrf.token}}">
    <button type="submit">Add To Driver Salary</button>
</form>

{{#response}}
    {{response}}
{{/response}}

<div>Orders with this driver</div>
<table>
    <thead>
    <tr>
        <th>OrderId</th>
        <th>CarId</th>
        <th>DriverId</th>
        <th>Amount of Passengers</th>
        <th>Distance</th>
        <th>Status</th>
        <th>UserId</th>
        <th></th>
    </tr>
    </thead>
    <tbody>
    {{#orders}}
        <tr>
            <td>{{orderId}}</td>
            <td>{{carId}}</td>
            <td>{{driverId}}</td>
            <td>{{amountOfPassengers}}</td>
            <td>{{distance}}</td>
            <td>{{status}}</td>
            <td>{{clientId}}</td>
        </tr>
    {{/orders}}
    </tbody>
</table>

</body>